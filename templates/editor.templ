package templates

import "app/config"

templ Editor() {
	<section>
		<div id="editorjs"></div>
		<script src={ config.Assets + "js/editor.js" }></script>
	</section>
}

templ EditorHeader() {
	<img src={ config.RootPrefix + "assets/default-banner.svg" }/>
	<div class="content">
		<h1>Mi Sitio</h1>
		<p>Crear un sitio</p>
	</div>
}
